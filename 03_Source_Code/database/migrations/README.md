Database migrations are implemented in `../backend/src/migrations/`.

That location remains the active runtime path used by TypeORM. This directory is
provided so the repository clearly exposes the database area expected by the
course instructions.
